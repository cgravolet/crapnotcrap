version: "3.9"
services:
    scraper:
        build: scraper/.
        volumes:
            - ./scraper/src:/src
            - ./mongo:/mongodata
    web:
        build: web/.
        ports:
            - "5000:5000"
        volumes:
            - ./web/src:/src
        environment:
            FLASK_ENV: development
    mongo:
        image: "mongo"
        volumes:
            - ./mongo:/mongodata
    redis:
        image: "redis:alpine"